---
mode: agent
description: Add a UI feature to the Avalonia app following the repo's MVVM pattern (ViewModel + compiled bindings + AsyncCommand + ViewModel tests). Use when adding a view, panel, button, or any user-facing action.
---

Read `.claude/skills/add-feature/SKILL.md` in this repository and follow the procedure it describes
exactly, applied to: ${input:task}

That file is the source of truth. Do not proceed from this prompt alone.
