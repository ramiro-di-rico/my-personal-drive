---
name: add-language
description: Add a new interface language to the app, or fill in missing translations for an existing one. Use when the UI needs to ship in another language, when a locale file has fallen behind English, or when new UI strings were added and every locale needs the new keys.
---

# add-language

The full procedure lives in the canonical, tool-agnostic file:

**`.claude/skills/add-language/SKILL.md`**

Read that file now and follow it exactly. Do not act from this summary alone — it is a pointer,
not the instructions. Edit the canonical file, never this one.
