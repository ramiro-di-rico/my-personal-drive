---
name: run-app
description: Launch the Avalonia app locally - against the real Proton Drive CLI or a stub CLI so the UI can be exercised without authenticating. Use when asked to run, start, or screenshot the app.
---

# run-app

The full procedure lives in the canonical, tool-agnostic file:

**`.claude/skills/run-app/SKILL.md`**

Read that file now and follow it exactly. Do not act from this summary alone — it is a pointer,
not the instructions. Edit the canonical file, never this one.
