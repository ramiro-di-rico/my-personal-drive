<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:syncVm="clr-namespace:MyPersonalDrive.ViewModels.Sync"
             x:Class="MyPersonalDrive.Views.SyncPanelView"
             x:DataType="syncVm:SyncPanelViewModel">

  <!-- Lifted verbatim out of MainWindow.axaml's settings ScrollViewer, where sync was the last
       section below the proton-drive executable path (docs/PLAN-UX-ROUND-2.md §5). First
       UserControl in the codebase: MainWindow.axaml was the only view file, which is also why the
       two breadcrumb bars are duplicated markup (§10). The extraction stops here on purpose. -->
  <ScrollViewer>
    <StackPanel Spacing="10" Margin="16">
    <TextBlock FontSize="18" FontWeight="Bold" Text="{Binding Loc[sync.title]}" />
    <Border Padding="16" Background="{DynamicResource CardBackgroundBrush}" CornerRadius="6">
      <StackPanel Spacing="12">
        <TextBlock Opacity="0.8"
                   TextWrapping="Wrap"
                   Text="{Binding Loc[sync.intro]}" />

        <StackPanel Orientation="Horizontal" Spacing="8">
          <Button Classes="primary" Command="{Binding AddPairCommand}">
            <StackPanel Orientation="Horizontal" Spacing="8">
              <Path Classes="icon" Data="{StaticResource IconPlus}" />
              <TextBlock Text="{Binding Loc[sync.addpair]}" />
            </StackPanel>
          </Button>
          <Button Command="{Binding RefreshCommand}">
            <StackPanel Orientation="Horizontal" Spacing="8">
              <Path Classes="icon" Data="{StaticResource IconRefresh}" />
              <TextBlock Text="{Binding Loc[common.refresh]}" />
            </StackPanel>
          </Button>
        </StackPanel>

        <!-- U7 (docs/PLAN-UX-ROUND-2.md §7): these toggles used to sit on the action row above,
             immediately over the filter chips below, rendered as same-sized pills naming the same
             providers — so two controls that do entirely different things (this one changes what
             the app DOES, that one changes what you SEE) read as one. Both rows are now labelled
             and given different affordances: a switch-shaped pill with a state dot here, flat
             chips there. -->
        <StackPanel Orientation="Horizontal" Spacing="10" VerticalAlignment="Center"
                    IsVisible="{Binding HasVisibleAccountSyncToggles}">
          <TextBlock Text="{Binding Loc[sync.autosync.label]}"
                     FontSize="12"
                     Opacity="0.7"
                     VerticalAlignment="Center" />
          <ItemsControl ItemsSource="{Binding VisibleAccountSyncToggles}">
            <ItemsControl.Styles>
              <Style Selector="Button.accountToggle">
                <Setter Property="Padding" Value="10,4" />
                <Setter Property="CornerRadius" Value="12" />
                <Setter Property="FontSize" Value="12" />
                <Setter Property="Background" Value="Transparent" />
                <Setter Property="BorderThickness" Value="1" />
                <Setter Property="BorderBrush" Value="{DynamicResource AccentMutedBrush}" />
              </Style>
            </ItemsControl.Styles>
            <ItemsControl.ItemsPanel>
              <ItemsPanelTemplate>
                <StackPanel Orientation="Horizontal" Spacing="8" />
              </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
              <DataTemplate x:DataType="syncVm:AccountSyncToggleViewModel">
                <Button Classes="accountToggle"
                        Command="{Binding ToggleCommand}"
                        ToolTip.Tip="{Binding ActionTooltip}">
                  <StackPanel Orientation="Horizontal" Spacing="6" VerticalAlignment="Center">
                    <Ellipse Width="7" Height="7" VerticalAlignment="Center">
                      <Ellipse.Styles>
                        <Style Selector="Ellipse.running">
                          <Setter Property="Fill" Value="{DynamicResource StatusOkBrush}" />
                        </Style>
                        <Style Selector="Ellipse.stopped">
                          <Setter Property="Fill" Value="{DynamicResource StatusOfflineBrush}" />
                        </Style>
                      </Ellipse.Styles>
                      <Classes.running>
                        <Binding Path="IsRunning" />
                      </Classes.running>
                      <Classes.stopped>
                        <Binding Path="!IsRunning" />
                      </Classes.stopped>
                    </Ellipse>
                    <TextBlock Text="{Binding Label}" VerticalAlignment="Center" />
                    <TextBlock Text="{Binding StateText}" Opacity="0.6" VerticalAlignment="Center" />
                  </StackPanel>
                </Button>
              </DataTemplate>
            </ItemsControl.ItemTemplate>
          </ItemsControl>
        </StackPanel>

        <!-- Filter by account (docs/PLAN-CLOUD-PROVIDERS.md P9) — same chip idiom as the
             browser's own "filter by type" row. Hidden with a single account, where every
             pair obviously belongs to it already. -->
        <StackPanel Orientation="Horizontal" Spacing="10" IsVisible="{Binding HasProviderFilters}">
          <TextBlock Text="{Binding Loc[sync.show.label]}"
                     FontSize="12"
                     Opacity="0.7"
                     VerticalAlignment="Center"
                     Margin="0,0,0,6" />
        <ItemsControl ItemsSource="{Binding ProviderFilters}">
          <ItemsControl.Styles>
            <Style Selector="Button.providerChip">
              <Setter Property="Padding" Value="8,3" />
              <Setter Property="FontSize" Value="12" />
              <Setter Property="Margin" Value="0,0,6,6" />
            </Style>
            <Style Selector="Button.providerChip.active">
              <Setter Property="Background" Value="{DynamicResource AccentMutedBrush}" />
              <Setter Property="Foreground" Value="{DynamicResource AccentBrush}" />
              <Setter Property="FontWeight" Value="Bold" />
            </Style>
          </ItemsControl.Styles>
          <ItemsControl.ItemsPanel>
            <ItemsPanelTemplate>
              <WrapPanel />
            </ItemsPanelTemplate>
          </ItemsControl.ItemsPanel>
          <ItemsControl.ItemTemplate>
            <DataTemplate x:DataType="syncVm:ProviderFilterViewModel">
              <Button Classes="providerChip"
                      Classes.active="{Binding IsActive}"
                      Command="{Binding ApplyCommand}"
                      Content="{Binding LabelWithCount}"
                      ToolTip.Tip="{Binding Loc[sync.filter.account.tooltip]}" />
            </DataTemplate>
          </ItemsControl.ItemTemplate>
        </ItemsControl>
        </StackPanel>

        <ItemsControl ItemsSource="{Binding VisiblePairs}">
          <ItemsControl.ItemTemplate>
            <DataTemplate x:DataType="syncVm:SyncPairViewModel">
              <Border Classes="hoverGroup" Padding="12" Margin="0,0,0,8" Background="{DynamicResource PanelBackgroundBrush}" CornerRadius="6">
                <Grid ColumnDefinitions="*,Auto" RowDefinitions="Auto,Auto">
                  <StackPanel Grid.Column="0" Grid.Row="0" Spacing="4">
                    <TextBlock FontSize="11"
                               Opacity="0.6"
                               FontWeight="Bold"
                               Text="{Binding AccountLabel}"
                               IsVisible="{Binding HasAccountLabel}" />
                    <TextBlock FontWeight="Bold" Text="{Binding RemotePath}" />
                    <TextBlock Opacity="0.7" TextWrapping="Wrap" Text="{Binding LocalPath}" />
                    <TextBlock Opacity="0.6" Text="{Binding DirectionText}" />
                    <TextBlock Text="{Binding StatusText}" />
                    <Button Content="{Binding ConflictText}"
                            IsVisible="{Binding HasConflicts}"
                            Command="{Binding ResolveConflictsCommand}"
                            HorizontalAlignment="Left"
                            Foreground="{DynamicResource StatusWarningBrush}"
                            ToolTip.Tip="{Binding Loc[sync.conflicts.tooltip]}" />
                    <!-- The detail view comes first and the blind retry-all second, deliberately:
                         the row used to offer only the second, so the user was asked to retry
                         without ever being told what failed (docs/PLAN-UX-ROUND-2.md §6). -->
                    <Button Content="{Binding FailureSummary}"
                            IsVisible="{Binding HasFailures}"
                            Command="{Binding ReviewFailuresCommand}"
                            HorizontalAlignment="Left"
                            Foreground="{DynamicResource StatusErrorBrush}"
                            ToolTip.Tip="{Binding Loc[sync.failures.tooltip]}" />
                    <Button Content="{Binding Loc[sync.retryfailed]}"
                            IsVisible="{Binding HasFailures}"
                            Command="{Binding RetryFailedCommand}"
                            HorizontalAlignment="Left"
                            ToolTip.Tip="{Binding Loc[sync.retryfailed.tooltip]}" />
                  </StackPanel>

                  <StackPanel Grid.Column="1" Grid.Row="0" Orientation="Horizontal" Spacing="8" VerticalAlignment="Top">
                    <Button Classes="rowAction" ToolTip.Tip="{Binding Loc[sync.preview.tooltip]}" Command="{Binding PreviewCommand}">
                      <Path Classes="icon" Data="{StaticResource IconEye}" />
                    </Button>
                    <Button Classes="rowAction" ToolTip.Tip="{Binding Loc[sync.edit.tooltip]}" Command="{Binding EditCommand}">
                      <Path Classes="icon" Data="{StaticResource IconPencil}" />
                    </Button>
                    <Button Classes="rowAction" ToolTip.Tip="{Binding Loc[menu.syncnow]}" Command="{Binding SyncNowCommand}">
                      <Path Classes="icon" Data="{StaticResource IconPlay}" />
                    </Button>
                    <Button Classes="rowAction" ToolTip.Tip="{Binding PauseTooltip}" Command="{Binding TogglePauseCommand}">
                      <Panel>
                        <Path Classes="icon" Data="{StaticResource IconPlay}" IsVisible="{Binding IsPaused}" />
                        <Path Classes="icon" Data="{StaticResource IconPause}" IsVisible="{Binding !IsPaused}" />
                      </Panel>
                    </Button>
                    <Button Classes="rowAction" ToolTip.Tip="{Binding Loc[sync.remove.tooltip]}" Command="{Binding RemoveCommand}">
                      <Path Classes="icon" Data="{StaticResource IconTrash}" />
                    </Button>
                  </StackPanel>

                  <ProgressBar Grid.Column="0" Grid.ColumnSpan="2" Grid.Row="1"
                               Margin="0,8,0,0"
                               IsIndeterminate="True"
                               IsVisible="{Binding IsBusy}" />
                </Grid>
              </Border>
            </DataTemplate>
          </ItemsControl.ItemTemplate>
        </ItemsControl>

        <!-- Only with no pairs at all. This prompt used to be the panel's initial StatusMessage,
             so it sat underneath three configured pairs telling the user to add their first one
             (docs/PLAN-UX-ROUND-2.md §13). -->
        <Border Padding="10"
                Background="{DynamicResource ConsoleBackgroundBrush}"
                CornerRadius="6"
                IsVisible="{Binding HasNoPairs}">
          <TextBlock TextWrapping="Wrap" Text="{Binding EmptyStateMessage}" Opacity="0.8" />
        </Border>

        <Border Padding="10"
                Background="{DynamicResource ConsoleBackgroundBrush}"
                CornerRadius="6"
                IsVisible="{Binding StatusMessage, Converter={x:Static StringConverters.IsNotNullOrEmpty}}">
          <TextBlock TextWrapping="Wrap" Text="{Binding StatusMessage}" />
        </Border>
      </StackPanel>
    </Border>
    </StackPanel>
  </ScrollViewer>
</UserControl>
